# 🎵 4duk Discord Radio Bot

Высокопроизводительный радио-бот для Discord на Go, который позволяет транслировать онлайн-радио прямо в голосовой канал.

---

## 🚀 Возможности

- 📡 Потоковое воспроизведение интернет-радио в голосовых каналах Discord
- 🟢 Простые команды для запуска и остановки трансляции
- 🔄 Автоматическое переподключение при обрыве соединения
- 🔒 Поддержка привилегированных интентов
- 🐳 Готов к запуску в Docker
- ⚡ Высокая производительность благодаря Go
- 🏗️ Чистая модульная архитектура

---

## 📁 Архитектура проекта

Проект организован по принципам чистой архитектуры:

```
.
├── cmd/
│   └── bot/              # Точка входа приложения
│       └── main.go
├── internal/
│   ├── audio/            # Аудио обработка
│   │   ├── constants.go  # Аудио константы
│   │   ├── encoder.go    # Opus кодировщик
│   │   └── streamer.go   # Потоковая передача аудио
│   ├── bot/              # Основная логика бота
│   │   ├── bot.go        # Основной тип Bot
│   │   ├── commands.go   # Обработка команд
│   │   ├── events.go      # Обработка событий Discord
│   │   ├── reconnect.go  # Логика переподключения
│   │   └── voice.go       # Работа с голосовыми каналами
│   ├── config/           # Конфигурация
│   │   └── config.go
│   └── radio/            # Управление состоянием радио
│       ├── manager.go    # Менеджер состояний
│       └── state.go      # Состояние радио для гильдии
├── Dockerfile
├── docker-compose.yml
├── go.mod
└── Makefile
```

### Модули:

- **cmd/bot** - точка входа приложения
- **internal/config** - загрузка и управление конфигурацией
- **internal/audio** - обработка аудио (Opus кодирование, потоковая передача)
- **internal/radio** - управление состоянием радио для каждой гильдии
- **internal/bot** - основная логика бота (команды, события, переподключение)

---

## 🛠️ Установка

### Требования

- Go 1.21 или выше
- FFmpeg (для обработки аудио)
- Discord Bot Token

### Локальная установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ankogit/4duk-discord-bot.git
cd 4duk-discord-bot
```

2. Установите зависимости:
```bash
make deps
# или
go mod download
```

3. Создайте файл `.env`:
```env
DISCORD_TOKEN=your_discord_bot_token_here
RADIO_URL=http://radio.4duk.ru/4duk128.mp3
```

4. Соберите и запустите:
```bash
make build
make run
# или
go build -o bin/bot ./cmd/bot
./bin/bot
```

### Docker

1. Создайте файл `.env` (см. выше)

2. Запустите с Docker Compose:
```bash
docker-compose up --build
```

Или используйте Makefile:
```bash
make docker-build
make docker-up
make docker-logs
```

---

## 📝 Команды бота

- `!join` - Подключает бота к голосовому каналу автора команды
- `!radio` - Включает радио в голосовом канале автора
- `!stop` - Останавливает радио и отключает бота

---

## ⚙️ Конфигурация

Переменные окружения:

- `DISCORD_TOKEN` (обязательно) - токен Discord бота
- `RADIO_URL` (опционально) - URL радиостанции (по умолчанию: `http://radio.4duk.ru/4duk128.mp3`)

---

## 🔧 Разработка

### Сборка

```bash
make build
```

### Запуск тестов

```bash
make test
```

### Очистка

```bash
make clean
```

---

## 🏗️ Архитектурные решения

1. **Модульность**: Код разделен на логические модули с четкими границами ответственности
2. **Контексты**: Использование `context.Context` для управления жизненным циклом горутин
3. **Безопасность**: Использование мьютексов для защиты конкурентного доступа
4. **Обработка ошибок**: Последовательная обработка ошибок с логированием
5. **Graceful shutdown**: Корректное завершение работы с очисткой ресурсов

---

## 📄 Лицензия

MIT

---

## 🤝 Вклад

Приветствуются Pull Request'ы и Issues!
